if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    max_val = max(arr)
    prev = -float('inf')
    for i in arr:
        if i>prev and i!=max_val:
            prev = i
            
    print(prev)
